class DeviceInfo {
  final String name;
  final String version;
  final String identifier;

  const DeviceInfo({
    required this.name,
    required this.version,
    required this.identifier,
  });
}